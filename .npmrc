# The `agents` SDK declares a peerOptional on vite >=6 while vitest 2 brings vite 5.
# That peer belongs to its dev server: it is used neither at runtime nor in the
# Worker bundle, so resolving with legacy-peer-deps keeps `npm ci` from failing
# with ERESOLVE over a dependency nothing here loads.
legacy-peer-deps=true
