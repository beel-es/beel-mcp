# El SDK `agents` declara un peerOptional de vite >=6, mientras vitest 2 trae vite 5.
# El peer es de su dev-server (no se usa en runtime ni en el bundle del Worker), así que
# resolvemos con legacy-peer-deps para que `npm ci` (local y CI) no falle con ERESOLVE.
legacy-peer-deps=true
