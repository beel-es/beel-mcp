# The bundled spec is a vendored artefact of the backend contract (see
# CONTRIBUTING.md). The Worker parses it at runtime, so it MUST travel in the
# repository — but it is never reviewed by hand: collapse it in diffs and keep it
# out of the language statistics.
openapi/public-api.yaml linguist-generated=true -diff

# Build output of the MCP App viewer; generated by scripts/build-mcpapp.mjs.
dist/mcpapp/* linguist-generated=true -diff
